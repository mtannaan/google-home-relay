# Make necessary changes and rename this to ".env"

# LOCAL=1
# DEBUG=1

# See README.md
TOKEN_CREATOR_JWT=00000000000

# Random secret to parse cookies. Tab-separated.
SESSION_SECRET=

# Local database for test purpose. Use docker and local-postgres/run.sh to set up one.
# After the first setup, run following commands to create required tables
#     psql $DATABASE_URL < node_modules/connect-pg-simple/table.sql
DATABASE_URL=postgres://postgres:pw@localhost:5432

# Genete using by bcryptjs.genSaltSync()
TOKEN_SALT=$2a$10$xxxxxxxxxxxx
